<UserControl x:Class="EventIAConstructor.Controls.TextBoxButton"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             x:Name="textBoxB" d:DesignWidth="137" d:DesignHeight="27"
             >
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="20"/>
        </Grid.ColumnDefinitions>

        <TextBox Name="textBox"
                 Text="{Binding ElementName=textBoxB, Path=Text, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}" />

        <Button Grid.Column="1" Content="..." Click="Button_Click"/>
    </Grid>
</UserControl>
